{
  "name": "@busirocket/agents-skills",
  "version": "1.0.0",
  "description": "A collection of reusable Agent Skills for TypeScript/React/Next.js/Rust/Tauri projects",
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\"",
    "validate": "node scripts/format-and-check.js --skip-format",
    "check": "node scripts/format-and-check.js --check-only",
    "check:format": "node scripts/format-and-check.js --check-only --skip-validate",
    "check:validate": "node scripts/format-and-check.js --check-only --skip-format"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BusiRocket/agents-skills.git"
  },
  "license": "MIT"
}
